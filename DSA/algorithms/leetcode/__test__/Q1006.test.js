import { clumsy } from "../Q1006";

describe("S0190", () => {
  test("S0190", () => {
    expect(clumsy(1)).toEqual(1);
    expect(clumsy(4)).toEqual(7);
    expect(clumsy(10)).toEqual(12);
  });
});
